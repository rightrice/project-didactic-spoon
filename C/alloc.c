#include <stdio.h>
#include <stdlib.h>

struct buffer {
	unsigned char* octets;
	int size;
};

typedef struct buffer Buffer;

int main( int argc, char * argv[]){
//	struct buffer * sum;
//	sum = (struct buffer *)malloc( sizeof (struct buffer));
	
	sum->size = count;
	sum->octects = (unsigned char*)malloc(sum->size);

	char * credit = "credit score";
	printf("\n");
	printf("credit worthiness is based on your %s.\n", credit);
	char * buf = credit;
	unsigned char * stetco = sum->octets;
	for(int position = 0; position < sum->size; position++){
		*stetco = *buf;
		stetco++;
		buf++;
	}
	
	
	
	exit (0);
}